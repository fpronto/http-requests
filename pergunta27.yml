info:
  name: pergunta27
  type: http
  seq: 1

http:
  method: GET
  url: "{{url3}}/ability/chlorophyll"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
