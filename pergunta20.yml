info:
  name: pergunta20
  type: http
  seq: 1

http:
  method: GET
  url: '{{url2}}/character?name="Abcxy123"'
  params:
    - name: name
      value: '"Abcxy123"'
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
