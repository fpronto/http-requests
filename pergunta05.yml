info:
  name: pergunta05
  type: http
  seq: 1

http:
  method: POST
  url: "{{url1}}/posts"
  body:
    type: json
    data: |-
      {
        "name": "titulo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​