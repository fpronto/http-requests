info:
  name: pergunta13
  type: http
  seq: 1

http:
  method: GET
  url: "{{url2}}/character?name=Rick&status=Dead"
  params:
    - name: name
      value: Rick
      type: query
    - name: status
      value: Dead
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
