info:
  name: pergunta26
  type: http
  seq: 1

http:
  method: GET
  url: "{{url3}}/pokemon?abilities=chlorophyll"
  params:
    - name: abilities
      value: chlorophyll
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
