info:
  name: pergunta12
  type: http
  seq: 1

http:
  method: GET
  url: "{{url2}}/character?name=Rick"
  params:
    - name: name
      value: Rick
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
