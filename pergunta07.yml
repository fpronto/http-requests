info:
  name: pergunta07
  type: http
  seq: 1

http:
  method: POST
  url: "{{url1}}/posts"
  body:
    type: json
    data: |-
      {
        "attrInvalido": "coisas invalidas"
        "id": "idInvalido"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
