info:
  name: pergunta29
  type: http
  seq: 1

http:
  method: GET
  url: "{{url3}}/pokemon/pikachu"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
