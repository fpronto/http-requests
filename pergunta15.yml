info:
  name: pergunta15
  type: http
  seq: 1

http:
  method: GET
  url: "{{url2}}/character?status=Alive&gender=Female&page=2"
  params:
    - name: status
      value: Alive
      type: query
    - name: gender
      value: Female
      type: query
    - name: page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
