info:
  name: pergunta30
  type: http
  seq: 1

http:
  method: GET
  url: https://pokeapi.co/api/v2/evolution-chain/10/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
