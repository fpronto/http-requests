info:
  name: pergunta03
  type: http
  seq: 1

http:
  method: GET
  url: "{{url1}}/posts?userId=3"
  params:
    - name: userId
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
